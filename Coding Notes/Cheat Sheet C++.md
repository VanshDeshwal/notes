## Array


* Find length of “ARRAY” :  
	`int length= ARRAY.size();`

* Sort an array:
	`sort(ARRAY.begin(),ARRAY.end());`

* Declare a new array of variable length and initialize all elements to zero:
	`vector<int>ARRAY(len,0);`

* Convert array into set: 
	`set<int>(ARRAY.begin(),ARRAY.end());`

* Compare String s1 with s2:
	`s1.compare(s2);`


